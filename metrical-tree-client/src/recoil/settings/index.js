import atom, { useSettings } from './atom';

// Add selectors in a file with the convention "with<Selector description>"
// import and export them here as named exports.

export { useSettings };
export default atom;
